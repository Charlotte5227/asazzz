<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="MauiGithubActionsSample.MainPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MauiGithubActionsSample"
    BackgroundColor="{AppThemeBinding Light=#FFFFFF, Dark=#121212}">

    <ContentPage.BindingContext>
        <local:MainViewModel />
    </ContentPage.BindingContext>

    <!-- ★スマホ縦で崩れないように：ページ全体をスクロールに -->
    <ScrollView>
        <Grid
            Padding="12"
            RowSpacing="10"
            ColumnSpacing="10"
            RowDefinitions="{OnIdiom Phone='Auto,Auto,Auto,Auto,Auto', Default='Auto,Auto,Auto,Auto'}"
            ColumnDefinitions="{OnIdiom Phone='*', Default='*,*'}">

            <!-- Top controls（全幅） -->
            <Frame Grid.Row="0" Grid.ColumnSpan="{OnIdiom Phone=1, Default=2}"
                   CornerRadius="12" Padding="12">
                <VerticalStackLayout Spacing="10">
                    <Label Text="軍事力・経済力 乱数アプリ（.NET MAUI）"
                           FontSize="18" FontAttributes="Bold" />

                    <!-- ★スマホでも収まるよう列構成を軽く -->
                    <Grid ColumnDefinitions="Auto,110" RowDefinitions="Auto,Auto,Auto" ColumnSpacing="12" RowSpacing="8">
                        <Label Grid.Row="0" Grid.Column="0" Text="日数（=処理回数）" VerticalTextAlignment="Center"/>
                        <Entry Grid.Row="0" Grid.Column="1" Keyboard="Numeric" Text="{Binding Days}" />

                        <HorizontalStackLayout Grid.Row="1" Grid.ColumnSpan="2" Spacing="8">
                            <CheckBox IsChecked="{Binding SyncME}" VerticalOptions="Center"/>
                            <Label Text="軍事Y↔経済Y 同期（同番号・同日）" VerticalTextAlignment="Center"/>
                        </HorizontalStackLayout>

                        <HorizontalStackLayout Grid.Row="2" Grid.ColumnSpan="2" Spacing="8">
                            <CheckBox IsChecked="{Binding SyncAllSlots}" VerticalOptions="Center"/>
                            <Label Text="全スロット同期（同じ日なら全部同じY）" VerticalTextAlignment="Center"/>
                        </HorizontalStackLayout>
                    </Grid>

                    <Grid ColumnDefinitions="*,*,*" ColumnSpacing="10">
                        <Button Text="🎲 生成" Clicked="OnGenerateClicked" />
                        <Button Text="➕ 合算" Clicked="OnSumClicked" />
                        <Button Text="🧹 結果リセット" Clicked="OnResetClicked" />
                    </Grid>

                    <Label Text="増加式：X + (|X|×Y%) ／ Yは小数OK ／ 最終値は四捨五入（整数）"
                           Opacity="0.7" FontSize="12"/>
                </VerticalStackLayout>
            </Frame>

            <!-- スロット欄：PC=左、スマホ=上 -->
            <Frame Grid.Row="1"
                   Grid.Column="{OnIdiom Phone=0, Default=0}"
                   CornerRadius="12" Padding="10">
                <VerticalStackLayout Spacing="10">
                    <Label Text="スロット欄" FontAttributes="Bold" FontSize="16"/>

                    <Grid ColumnDefinitions="*,*,*,*" ColumnSpacing="8">
                        <Button Text="＋軍事" Clicked="OnAddMilitarySlot" />
                        <Button Text="＋経済" Clicked="OnAddEconomySlot" />
                        <Button Text="軍事削除" Clicked="OnRemoveMilitarySlot" />
                        <Button Text="経済削除" Clicked="OnRemoveEconomySlot" />
                    </Grid>

                    <!-- ★ここはページ全体がScrollViewなので、内側は基本スクロール無しが扱いやすい -->
                    <VerticalStackLayout Spacing="12">
                        <Label Text="【軍事】" FontAttributes="Bold" />
                        <CollectionView ItemsSource="{Binding MilitarySlots}" SelectionMode="None">
                            <CollectionView.ItemTemplate>
                                <DataTemplate>
                                    <Frame CornerRadius="10" Padding="10" Margin="0,0,0,8">
                                        <Grid RowDefinitions="Auto,Auto" ColumnDefinitions="Auto,*" ColumnSpacing="12" RowSpacing="6">
                                            <Label Grid.Row="0" Grid.Column="0" Text="{Binding Title}" FontAttributes="Bold"/>
                                            <HorizontalStackLayout Grid.Row="0" Grid.Column="1" Spacing="8">
                                                <Label Text="最大値" VerticalTextAlignment="Center"/>
                                                <Entry WidthRequest="100" Keyboard="Numeric" Text="{Binding MaxValue}" />
                                            </HorizontalStackLayout>

                                            <HorizontalStackLayout Grid.Row="1" Grid.ColumnSpan="2" Spacing="18">
                                                <HorizontalStackLayout Spacing="6">
                                                    <CheckBox IsChecked="{Binding UseSign}" />
                                                    <Label Text="増減判定" VerticalTextAlignment="Center"/>
                                                </HorizontalStackLayout>
                                                <HorizontalStackLayout Spacing="6">
                                                    <CheckBox IsChecked="{Binding UseIncrease}" />
                                                    <Label Text="増加(Y適用)" VerticalTextAlignment="Center"/>
                                                </HorizontalStackLayout>
                                            </HorizontalStackLayout>
                                        </Grid>
                                    </Frame>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>

                        <Label Text="【経済】" FontAttributes="Bold" />
                        <CollectionView ItemsSource="{Binding EconomySlots}" SelectionMode="None">
                            <CollectionView.ItemTemplate>
                                <DataTemplate>
                                    <Frame CornerRadius="10" Padding="10" Margin="0,0,0,8">
                                        <Grid RowDefinitions="Auto,Auto" ColumnDefinitions="Auto,*" ColumnSpacing="12" RowSpacing="6">
                                            <Label Grid.Row="0" Grid.Column="0" Text="{Binding Title}" FontAttributes="Bold"/>
                                            <HorizontalStackLayout Grid.Row="0" Grid.Column="1" Spacing="8">
                                                <Label Text="最大値" VerticalTextAlignment="Center"/>
                                                <Entry WidthRequest="100" Keyboard="Numeric" Text="{Binding MaxValue}" />
                                            </HorizontalStackLayout>

                                            <HorizontalStackLayout Grid.Row="1" Grid.ColumnSpan="2" Spacing="18">
                                                <HorizontalStackLayout Spacing="6">
                                                    <CheckBox IsChecked="{Binding UseSign}" />
                                                    <Label Text="増減判定" VerticalTextAlignment="Center"/>
                                                </HorizontalStackLayout>
                                                <HorizontalStackLayout Spacing="6">
                                                    <CheckBox IsChecked="{Binding UseIncrease}" />
                                                    <Label Text="増加(Y適用)" VerticalTextAlignment="Center"/>
                                                </HorizontalStackLayout>
                                            </HorizontalStackLayout>
                                        </Grid>
                                    </Frame>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </VerticalStackLayout>
                </VerticalStackLayout>
            </Frame>

            <!-- 結果欄：PC=右、スマホ=スロットの次 -->
            <Frame Grid.Row="{OnIdiom Phone=2, Default=1}"
                   Grid.Column="{OnIdiom Phone=0, Default=1}"
                   CornerRadius="12" Padding="10">
                <VerticalStackLayout Spacing="10">
                    <Label Text="結果欄" FontAttributes="Bold" FontSize="16"/>
                    <CollectionView ItemsSource="{Binding Results}" SelectionMode="None">
                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                <Frame CornerRadius="12" Padding="12" Margin="0,0,0,8">
                                    <VerticalStackLayout Spacing="8">
                                        <Label Text="{Binding Day, StringFormat='{0}日目'}" FontAttributes="Bold" />
                                        <Label Text="軍事（各枠）" Opacity="0.7" FontSize="12"/>
                                        <Label Text="{Binding MilitaryText}" />
                                        <Label Text="経済（各枠）" Opacity="0.7" FontSize="12"/>
                                        <Label Text="{Binding EconomyText}" />
                                        <Label Text="{Binding MilitarySum, StringFormat='軍事合計: {0}'}" FontAttributes="Bold"/>
                                        <Label Text="{Binding EconomySum, StringFormat='経済合計: {0}'}" FontAttributes="Bold"/>
                                    </VerticalStackLayout>
                                </Frame>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </VerticalStackLayout>
            </Frame>

            <!-- Y入力欄：全幅 -->
            <Frame Grid.Row="{OnIdiom Phone=3, Default=2}" Grid.ColumnSpan="{OnIdiom Phone=1, Default=2}"
                   CornerRadius="12" Padding="10">
                <!-- ★ここはあなたの既存のままでOK：ただし入れ子Scrollが多いので後で整理推奨 -->
                <VerticalStackLayout Spacing="10">
                    <Label Text="増加率Y[%] 入力（横：1日目→2日目→…）" FontAttributes="Bold" FontSize="16"/>

                    <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                        <Button x:Name="TabMil" Text="軍事Y[%]" Clicked="OnTabMil" />
                        <Button x:Name="TabEco" Text="経済Y[%]" Clicked="OnTabEco" />
                    </Grid>

                    <ContentView x:Name="MilPanel">
                        <CollectionView ItemsSource="{Binding MilitarySlots}" SelectionMode="None">
                            <CollectionView.ItemTemplate>
                                <DataTemplate>
                                    <Frame CornerRadius="12" Padding="10" Margin="0,0,0,8">
                                        <VerticalStackLayout Spacing="6">
                                            <Label Text="{Binding Title}" FontAttributes="Bold"/>
                                            <ScrollView Orientation="Horizontal">
                                                <HorizontalStackLayout Spacing="8">
                                                    <CollectionView ItemsSource="{Binding YCells}"
                                                                    ItemsLayout="HorizontalList"
                                                                    SelectionMode="None">
                                                        <CollectionView.ItemTemplate>
                                                            <DataTemplate>
                                                                <Frame CornerRadius="10" Padding="6" Margin="0">
                                                                    <Entry Keyboard="Numeric" WidthRequest="90" Text="{Binding Value}" />
                                                                </Frame>
                                                            </DataTemplate>
                                                        </CollectionView.ItemTemplate>
                                                    </CollectionView>
                                                </HorizontalStackLayout>
                                            </ScrollView>
                                        </VerticalStackLayout>
                                    </Frame>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </ContentView>

                    <ContentView x:Name="EcoPanel" IsVisible="False">
                        <CollectionView ItemsSource="{Binding EconomySlots}" SelectionMode="None">
                            <CollectionView.ItemTemplate>
                                <DataTemplate>
                                    <Frame CornerRadius="12" Padding="10" Margin="0,0,0,8">
                                        <VerticalStackLayout Spacing="6">
                                            <Label Text="{Binding Title}" FontAttributes="Bold"/>
                                            <ScrollView Orientation="Horizontal">
                                                <HorizontalStackLayout Spacing="8">
                                                    <CollectionView ItemsSource="{Binding YCells}"
                                                                    ItemsLayout="HorizontalList"
                                                                    SelectionMode="None">
                                                        <CollectionView.ItemTemplate>
                                                            <DataTemplate>
                                                                <Frame CornerRadius="10" Padding="6">
                                                                    <Entry Keyboard="Numeric" WidthRequest="90" Text="{Binding Value}" />
                                                                </Frame>
                                                            </DataTemplate>
                                                        </CollectionView.ItemTemplate>
                                                    </CollectionView>
                                                </HorizontalStackLayout>
                                            </ScrollView>
                                        </VerticalStackLayout>
                                    </Frame>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </ContentView>
                </VerticalStackLayout>
            </Frame>

            <!-- 合算欄：全幅 -->
            <Frame Grid.Row="{OnIdiom Phone=4, Default=3}" Grid.ColumnSpan="{OnIdiom Phone=1, Default=2}"
                   CornerRadius="12" Padding="12">
                <VerticalStackLayout Spacing="10">
                    <Label Text="合算結果表示欄（軍事合計 / 経済合計）" FontAttributes="Bold" FontSize="16"/>

                    <Grid ColumnDefinitions="Auto,140,Auto,140" ColumnSpacing="12" RowSpacing="8">
                        <Label Text="軍事 初期値" VerticalTextAlignment="Center"/>
                        <Entry Text="{Binding InitMilitary}" Keyboard="Numeric" />

                        <Label Text="経済 初期値" VerticalTextAlignment="Center"/>
                        <Entry Text="{Binding InitEconomy}" Keyboard="Numeric" />
                    </Grid>

                    <Label Text="{Binding SumText}" FontSize="14" FontAttributes="Bold"/>
                </VerticalStackLayout>
            </Frame>

        </Grid>
    </ScrollView>
</ContentPage>
